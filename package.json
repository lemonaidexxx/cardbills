{
  "name": "cardbills",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "deploy": "wrangler deploy",
    "check": "node tools/check.mjs",
    "secrets": "node tools/generate-secrets.mjs",
    "pretest": "node tools/build.mjs",
    "precheck": "node tools/build.mjs",
    "build": "node tools/build.mjs"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "devDependencies": {
    "wrangler": "4.102.0"
  }
}
